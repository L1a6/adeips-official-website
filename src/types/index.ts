export interface Testimonial {
  image: string;
  name: string;
  role: string;
  quote: string;
}

export interface FAQ {
  question: string;
  answer: string;
}

export interface IconicMoment {
  title: string;
  description: string;
  image: string;
  buttonText: string;
  buttonColor: string;
}